//: Playground - noun: a place where people can play

import UIKit

/**
 Markdown comment syntax. Describe function here.
*/
func myFunc() {
    return
}

// use backticks (`) to mark code
// bullets: * + a space
// indent asterisks to make sublists
// write numbered lists starting with 1. subsequent items can also be numbered 1. and Xcode will renumber automatically!
// write a link: [text in brackets](link in parenthesis)
// # heading, ##subheading, ### sub-subheading
// *word* = italic, **word** = bold
// Returns: what caller can expect back if func runs successfully
// Parameter: specify parameter name + descrive contents, ex. Parameter breed: The breed of a cat.
// Throws: Error types that can be thrown by func
// Precondition: describes correct state of program before function is called
// Complexity: Big O notation
// Authors: Function's author
